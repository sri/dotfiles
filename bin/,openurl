#!/usr/bin/env python3
import sys, os, webbrowser

if len(sys.argv) != 2:
    sys.exit(f"usage: {sys.argv[0]} arg")
arg = sys.argv[1]
if os.path.exists(arg) or arg.startswith("http"):
    url = arg
else:
    url = f"https://{arg}"
webbrowser.open(url)
