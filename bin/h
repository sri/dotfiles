#!/bin/bash
os=`uname`
url=
case "$os" in
    Darwin* ) url=`pbpaste`  ;;
    Linux*  ) url=`xclip -o` ;;
esac
if [ "$url" = "" ]; then
    echo "(nothing in url)"
fi
echo $url
curl --silent --head "$url"
